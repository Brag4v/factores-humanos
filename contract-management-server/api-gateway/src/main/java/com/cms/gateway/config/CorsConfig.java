package com.cms.gateway.config;

// CORS is handled exclusively via globalcors in application.yml.
// The CorsWebFilter bean was removed to prevent duplicate Access-Control-Allow-Origin headers.
public class CorsConfig {
}
